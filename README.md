# Experiment on Ensemble Kernels

## Results

Check the folder `experiemnt`, under which results on each dataset are display with 

- configuration in `para.yml`
- compared metrics in `results.json`
- linear weights in `weights.txt`.

